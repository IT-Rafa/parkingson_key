
/// Valores por defecto de accesibilidad del teclado
class KeyboardAccessibilityConfig {
  static const Duration acceptHoldDuration =
      Duration(milliseconds: 500);

  static const Duration repeatBlockDuration =
      Duration(milliseconds: 300);

  static const bool hapticEnabled = true;
}
